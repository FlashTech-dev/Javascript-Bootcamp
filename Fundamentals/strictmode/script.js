//try running the first multiline comment first by removing the /* -- */ and check the console.log you will see no errors//

/* //'use strict';//
let hasAge= false;
const vote =true;
if(vote) hasAg= true;
if(hasAge) console.log('you can vote');
*/

// now try running this//
/* 'use strict';
let hasAge= false;
const vote =true;
if(vote) hasAg= true;  //wrong variable name wrote on purpose 
if(hasAge) console.log('you can vote');
*/

// you must have seen the error thrown in the console after you open the index.html file//